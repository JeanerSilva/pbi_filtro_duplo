import "./../style/visual.less";
import powerbi from "powerbi-visuals-api";
import IVisual = powerbi.extensibility.visual.IVisual;
import VisualConstructorOptions = powerbi.extensibility.visual.VisualConstructorOptions;
import VisualUpdateOptions = powerbi.extensibility.visual.VisualUpdateOptions;
export declare class Visual implements IVisual {
    private host;
    private root;
    private searchBar;
    private searchInput;
    private searchClear;
    private listContainer;
    private treeRootEl;
    private settings;
    private categoryColumns;
    private targets;
    private rootNode;
    private levelsCount;
    private searchQuery;
    private lastSignature;
    private activeSelectionLevel;
    constructor(options?: VisualConstructorOptions);
    update(options: VisualUpdateOptions): void;
    private rebuildTree;
    private findChild;
    private collectSelectedByPath;
    private restoreSelectionByPath;
    private renderTree;
    private onNodeClick;
    private clearAllSelections;
    private clearSelectionsExceptLevel;
    private anySelected;
    private findFirstLeaf;
    private applyBasicFilterForLevel;
    private extractTargetFromMetadata;
    private applySearchBarSettings;
    private matchesSearch;
    private descendantMatchesSearch;
    private walk;
    enumerateObjectInstances(options: powerbi.EnumerateVisualObjectInstancesOptions): powerbi.VisualObjectInstanceEnumeration;
}
